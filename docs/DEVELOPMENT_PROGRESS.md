# 开发进度

更新时间：`2026-07-11 11:20 CST (Asia/Shanghai)`

## 项目阶段

| 平台 | 状态 | 当前交付 |
|---|---|---|
| Windows x64 | 已完成可测试版本 | 独立 `.exe`，三步界面和打开结果目录功能 |
| macOS arm64 | 待开始 | 目标为 M5 MacBook Air / macOS 26 的 `.app` 和 `.dmg` |
| 正式签名分发 | 暂缓 | 当前没有 Apple Developer 账户 |

## 已完成

### 业务流程

- 第 1 步：DP 订单 CSV 转云途批量寄件模板。
- 第 2 步：云途订单信息转 DP 发货回填模板。
- 第 3 步：根据运单号和 SKU 分拣面单。
- SKU 文件夹命名：`日期+SKU+申报中文名+数量`。
- 每个 SKU 文件夹同时包含 PDF 和供应商 ZIP。
- 单个 SKU 文件夹不放发货明细表。
- 操作结果按发货日期保存。

### Windows 界面

- 三步流程导航。
- 日期默认采用系统当前日期并允许修改。
- SKU 商品库和模板收纳到固定配置。
- 本次批次文件突出展示。
- 自动识别下载目录中的最新文件。
- 处理成功后可以直接打开结果文件夹。
- 三个步骤在默认 `1120 x 620` 窗口中完整显示。
- 2026-07-11 更新为浅色雾蓝、系统蓝强调色的 OS 26 风格界面。

### Windows 验证

- `app.py` 通过 Python 语法编译检查。
- 三个步骤完成 Tk 窗口尺寸检查。
- PyInstaller 独立 `.exe` 构建成功。
- 新构建的 `.exe` 已启动检查，没有启动后异常退出。

## macOS 下一阶段

1. 在 M5 MacBook Air 上解压或克隆仓库。
2. 使用原生 arm64 Python 创建虚拟环境。
3. 从源码运行现有程序。
4. 替换 Windows 专属的 `explorer` 调用。
5. 统一四处输出路径逻辑。
6. 增加固定配置持久化和自定义输出根目录。
7. 检查 macOS 26 字体、窗口尺寸和中文路径。
8. 使用脱敏测试数据验证三个步骤。
9. 用 PyInstaller 构建 arm64 `.app`。
10. 进行临时签名并制作本机测试版 `.dmg`。

详细要求见 [`../MacBook_M5_macOS26开发交接文档.md`](../MacBook_M5_macOS26开发交接文档.md)。

## 当前已知限制

- `app.py` 的打开文件夹操作仍使用 Windows `explorer`。
- 多个脚本各自定义输出路径，尚未抽取为共享模块。
- 固定配置路径尚未写入跨平台配置文件。
- Tkinter 可以实现接近 OS 26 的配色和层级，但无法实现完整原生液态玻璃效果。
- 没有 Apple Developer 账户，因此当前 Mac 版本只能先做本机测试和临时签名。

## 变更记录

### 2026-07-11

- 完成 Windows OS 26 风格浅色界面重构。
- 消除主工作区无意义留白，运行日志区自适应剩余空间。
- 完成 Windows `.exe` 重新构建和启动验证。
- 创建 M5 / macOS 26 开发交接文档。
- 创建私有 GitHub 仓库的首次源码、文档和进度导入资料。

### 2026-07-10

- 优化三步骤界面，隐藏步骤 2、3 中自动沿用的文件，避免误以为需要重复提交。
- 增加帮助提示图标、打开结果文件夹和默认当前日期。
- 修复 DP CSV 表头识别兼容问题。

### 2026-07-09

- 使用真实五单流程验证云途上传模板和 DP 回填模板。
- 验证按 SKU 分拣云途 PDF 面单。
- 确认供应商目录命名与输出归档规则。

## 接手规则

- 新 Codex 开始工作前先更新本文件顶部时间戳。
- 每次提交都在“变更记录”顶部追加日期和内容。
- 字段映射不确定时必须询问，不允许猜测。
- 不得在日志、截图、Issue 或提交中暴露买家个人信息。
- Mac 端业务逻辑修改后，必须记录与 Windows 输出的字段级对比结果。

