# 开发进度

更新时间：`2026-07-26 23:45 CST (Asia/Shanghai)`

## 项目阶段

| 平台 | 状态 | 当前交付 |
|---|---|---|
| Windows x64 | 可用测试版 | 云途与顺丰三步流程均已接入并经用户确认操作结果正确 |
| macOS arm64 | 待实机构建 | 共用 Windows 界面与业务逻辑；Mac Finder 适配、依赖及 arm64 PyInstaller 配置已完成 |
| Web | 可部署测试版 | 双平台三步流程、多账号隔离、Docker 部署配置和真实文件回归已完成 |
| 正式签名分发 | 暂缓 | 当前没有 Apple Developer 账户 |

## 已完成

### 业务流程

- 第 1 步：DP 订单 CSV 转云途批量寄件模板。
- 第 2 步：云途或顺丰国际订单信息转 DP 发货回填模板。
- 第 2 步自动复用第 1 步 DP CSV，只比较 `processing` 订单，并要求承运商订单号集合完全一致；用户无需重复选择 DP CSV。
- 第 3 步：云途要求 DP、云途订单和面单集合完全一致后按 SKU 分拣；顺丰要求第 2 步运单与 PDF 面单完全一致后按寄方姓名拆分。
- SKU 文件夹命名：`日期+SKU+申报中文名+数量`。
- 每个 SKU 文件夹同时包含 PDF 和供应商 ZIP。
- 单个 SKU 文件夹不放发货明细表。
- 操作结果按发货日期保存。

### Windows 界面

- 三步流程导航。
- 日期默认采用系统当前日期并允许修改。
- SKU 商品库和模板收纳到固定配置。
- 本次批次文件突出展示。
- 自动识别下载目录中的最新文件。
- 处理成功后可以直接打开结果文件夹。
- 三个步骤在默认 `1120 x 620` 窗口中完整显示。
- 2026-07-11 更新为浅色雾蓝、系统蓝强调色的 OS 26 风格界面。

### Windows 验证

- `app.py` 通过 Python 语法编译检查。
- 三个步骤完成 Tk 窗口尺寸检查。
- PyInstaller 独立 `.exe` 构建成功。
- 新构建的 `.exe` 已启动检查，没有启动后异常退出。
- Windows PyInstaller 配置已移除本机绝对路径，可在其他仓库位置构建。
- 最新 Windows 包：`DP-International-Fulfillment-To-Au_Windows_20260726_2312.zip`；SHA-256：`FF14C1A6B3AC553EABB3EBD7B3D02B79611B9A5E00FFE0134AB30A1EFE0E4117`。

### 顺丰国际

- 第 1 步新增“云途 / 顺丰国际”发货平台选择。
- 已读取并匹配 `BatchImportOrders_V39.xlsm` 的 102 列标准上传格式。
- 顺丰模式要求用户明确选择业务类型和是否带电，避免程序猜测渠道。
- 已映射 DP 订单号、收件信息、AU 地址、电话、重量和商品申报字段。
- 使用 XML 级写入保留原始 `.xlsm` 包结构、样式、下拉验证和自定义内容。
- 12 单虚构数据测试通过，覆盖超过模板预留行数的扩展场景。
- 更新后的 Windows `.exe` 已启动，窗口默认尺寸为 `1120 x 620`。
- 顺丰第 1 步已经用户实际后台上传验证成功。
- 顺丰第 2 步读取“客户订单号”和“顺丰运单号”，DP 承运商固定为 `SF INTERNATIONAL`。
- 原生支持顺丰 `.xls/.xlsx`，用户不需要手动转换旧版 `.xls`。
- 246 单实际顺丰导出验证通过：订单号、运单号均无空值、无重复，承运商数量一致。
- 新批次校验发现当前 247 单 DP CSV 与 246 单顺丰导出存在 2 个缺失、1 个额外订单，并正确拒绝生成，证明旧版本存在的错批次风险已被封堵。
- 顺丰第 3 步支持合并 PDF，按每页“寄方姓名”生成供应商 PDF。
- 顺丰第 3 步自动复用第 2 步订单数据，要求订单运单号与 PDF 运单号一个不少、一个不多，界面仍只需选择 PDF。
- 246 页面单验证通过：寄方姓名无缺失、运单号无重复，拆分为 245 页和 1 页两个 PDF。
- 原始和拆分后的面单已完成首页、末页视觉渲染检查，条码、二维码及版式保持不变。

## macOS 下一阶段

1. 在 M5 MacBook Air 上解压或克隆仓库。
2. 使用原生 arm64 Python 创建虚拟环境。
3. 运行 `desktop/macos/app.py`，检查 macOS 26 字体、窗口尺寸和中文路径。
4. 分别使用云途与顺丰脱敏数据验证三个步骤。
5. 对比 Mac 与 Windows 输出中的订单号、运单号、SKU 和数量。
6. 运行 `build_macos_arm64.sh` 构建 arm64 `.app`。
7. 进行临时签名并制作本机测试版 `.dmg`。

详细要求见 [`../desktop/macos/MACOS_M5_HANDOFF_ZH.md`](../desktop/macos/MACOS_M5_HANDOFF_ZH.md)。

## 当前已知限制

- Windows、Mac 和 Web 的入口已经分开，业务脚本统一位于 `shared/fulfillment/`。
- 多个脚本各自定义输出路径，尚未抽取为共享模块。
- Mac 固定配置路径沿用桌面版自动识别逻辑，尚未在 Mac 实机验证持久化体验。
- Tkinter 可以实现接近 OS 26 的配色和层级，但无法实现完整原生液态玻璃效果。
- 没有 Apple Developer 账户，因此当前 Mac 版本只能先做本机测试和临时签名。
- Web 将耗时处理放入后台线程，但为保护共享脚本参数仍采用单进程串行任务队列，适合低并发；高并发需要独立任务队列、PostgreSQL 和对象存储。
- Web 尚未实现登录限流、CSRF 令牌、密码重置、自动过期清理和完整安全审计。
- DP 一个订单包含多个 SKU 时仍会明确拒绝自动处理，需要先确定拆包和供应商归属规则。
- 当前 Windows 环境没有 Docker，部署包已做静态与应用级验证，但 Docker 镜像仍需在 Linux 服务器实际构建。

## 变更记录

### 2026-07-26

- 完成 GitHub 同步完整性复核：发现草稿 PR `#2` 中的 `web/app.py` 和 `desktop/windows/app.py` 被截断提示污染，现以本机最终源码完整恢复，避免损坏版本进入 `main`。
- 当前本机最终源码重新执行 23 项自动化测试并全部通过；GitHub 修复后仍需在服务器重新构建容器，线上版本不会仅因提交代码自动更新。
- 线上 `https://dpz.zylgzx.cn/healthz` 在复核时仍返回 404，确认网站运行的是不含顺丰功能的旧部署。
- 完成遗留问题审查：第 2 步新增 DP/承运商订单集合强校验，避免误传上一批订单后仍生成 DP 回填表。
- Web 失败上传不再覆盖上一份已验证批次；固定配置改为全部校验通过后再替换，并返回可读的 400 错误页。
- Web 耗时脚本移至线程池，处理任务串行保护，登录和健康检查不会被文件处理阻塞。
- DP CSV 与云途面单分拣统一只读取 `processing` 订单；补齐共享脚本的 Excel 工作簿关闭。
- 浏览器日期改为使用用户电脑本地日期；启动时清理过期登录会话。
- 新增项目根目录 `.dockerignore`，部署 ZIP 不再包含 `__pycache__/.pyc`，并确认无真实 CSV、Excel、PDF、数据库或 `.env`。
- 23 项自动化测试通过；五单云途完整三步及双向集合校验通过；顺丰 246 单错批次拦截、回填、运单集合核对和 246 页面单拆分通过。
- 生成 Web 部署包 `DP-International-Fulfillment-Web_20260726_2313.zip`，SHA-256 为 `A0B8A3C39F6A25F7F2626B244F739F450228FCD2D38D93900592CA7AA5AEC464`。
- macOS 新增可运行入口、Finder 目录打开适配、依赖文件及 Apple Silicon PyInstaller 配置。
- macOS 直接复用 Windows 已验证的云途/顺丰三步界面及 `shared/fulfillment/` 业务逻辑。
- Web 三个步骤全部增加云途/顺丰国际切换；顺丰第 1 步增加业务类型和是否带电。
- Web 顺丰第 2 步支持 `.xls/.xlsx` 并固定承运商为 `SF INTERNATIONAL`；第 3 步支持合并 PDF 按寄方姓名拆分。
- Web 固定配置增加顺丰国际模板，并允许单独替换任意一份配置。
- Web 历史记录、工作目录和员工下载权限按账号隔离；脚本执行增加并发锁。
- 增加 `/healthz`、非 root Docker 用户和 Compose 健康检查。
- 早期 14 项自动化测试与 `DP-International-Fulfillment-Web_20260726_2136.zip` 已由本次 23 项测试和最新部署包取代。
- 最新源码已同步到 GitHub 分支 `agent/sf-mac-web-20260726`，对应 PR 为 `#2`。
- Windows 第 3 步增加云途/顺丰国际切换；顺丰模式只需提供一个合并面单 PDF。
- 新增顺丰面单按“寄方姓名”拆分，输出每个寄方的合并 PDF、分组汇总和校验报告。
- 增加 `pypdf` 依赖并完成 246 页面单拆分与视觉验证。
- 顺丰第 1 步经用户确认已成功上传顺丰后台。
- Windows 第 2 步增加云途/顺丰国际切换，顺丰模式只显示本次顺丰订单数据。
- 顺丰回填映射为“客户订单号 -> Order ID”“顺丰运单号 -> Tracking Number”。
- DP 承运商使用允许列表中的精确值 `SF INTERNATIONAL`。
- 增加 `xlrd`，支持直接读取顺丰导出的 Excel 97-2003 `.xls`。
- 使用 246 单实际顺丰导出生成 DP 回填文件并完成完整性校验。
- 新增顺丰国际批量上传模板生成器。
- Windows 第 1 步增加云途/顺丰国际平台切换。
- 固定配置增加顺丰国际标准模板。
- 顺丰模式增加 16 个业务类型选择和是否带电选择。
- 完成 12 单字段校验、工作簿结构校验、下拉验证校验和视觉渲染检查。

### 2026-07-11

- 完成 Windows OS 26 风格浅色界面重构。
- 消除主工作区无意义留白，运行日志区自适应剩余空间。
- 完成 Windows `.exe` 重新构建和启动验证。
- 创建 M5 / macOS 26 开发交接文档。
- 创建私有 GitHub 仓库的首次源码、文档和进度导入资料。
- 按 Windows、macOS、Web、共享业务逻辑重新整理代码目录，并验证两个程序入口可找到共享脚本。

### 2026-07-10

- 优化三步骤界面，隐藏步骤 2、3 中自动沿用的文件，避免误以为需要重复提交。
- 增加帮助提示图标、打开结果文件夹和默认当前日期。
- 修复 DP CSV 表头识别兼容问题。

### 2026-07-09

- 使用真实五单流程验证云途上传模板和 DP 回填模板。
- 验证按 SKU 分拣云途 PDF 面单。
- 确认供应商目录命名与输出归档规则。

## 接手规则

- 新 Codex 开始工作前先更新本文件顶部时间戳。
- 每次提交都在“变更记录”顶部追加日期和内容。
- 字段映射不确定时必须询问，不允许猜测。
- 不得在日志、截图、Issue 或提交中暴露买家个人信息。
- Mac 端业务逻辑修改后，必须记录与 Windows 输出的字段级对比结果。
